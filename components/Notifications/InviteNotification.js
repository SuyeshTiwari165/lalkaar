import React from 'react';
import { View, Text } from 'react-native';

const InviteNotification = ({ message }) => {
  return (
    <View>
      <Text>{message}</Text>
    </View>
  );
};

export default InviteNotification;
